#!/usr/bin/with-contenv bashio
# shellcheck shell=bash
set -e

cd /app || bashio::exit.nok "Could not cd to /app"

bashio::log.info "Starting HA Dashboard Studio..."

exec python3 -m uvicorn dashboard_studio.main:app --host 0.0.0.0 --port 8099
